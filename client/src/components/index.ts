export * from './layout'; // we are using * to export all the  layout components from the layout folder

export { default as Post } from './post';
export { default as CommonModal } from './modal';
export { default as CommonFileInput } from './fileInput';
